% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{dag_layout}
\alias{dag_layout}
\title{DAG layout}
\usage{
dag_layout(
  rankdir = c("TB", "LR", "BT", "RL"),
  nodesep = 50,
  ranksep = 50,
  ranker = c("network-simplex", "tight-tree", "longest-path"),
  node_size = NULL,
  sort_by_combo = TRUE
)
}
\arguments{
\item{rankdir}{Flow direction: \code{"TB"} (default), \code{"LR"}, \code{"BT"} or \code{"RL"}.
The DAG's edge curve and port sides are derived from this.}

\item{nodesep}{Separation between nodes in the same rank, in px.}

\item{ranksep}{Separation between ranks, in px.}

\item{ranker}{Rank-assignment algorithm: \code{"network-simplex"} (default),
\code{"tight-tree"} or \code{"longest-path"}.}

\item{node_size}{Collision box \code{c(width, height)} (or a single number) used
for spacing. Set the width to roughly the widest label so labels don't
overlap. \code{NULL} (default) leaves dagre's own spacing for top-down flow and
reserves label width for the left-to-right flows where it's needed.}

\item{sort_by_combo}{Keep stacked (combo) nodes together during layout.}
}
\value{
A g6 \code{antv-dagre} layout (a plain list), suitable for
\code{\link[=new_dag_extension]{new_dag_extension()}} and JSON-serialisable for save / restore.
}
\description{
Layout for the DAG view, used as the \code{layout} argument of
\code{\link[=new_dag_extension]{new_dag_extension()}}. blockr DAGs are directed, multi-parent graphs, so the
only sensible layouts are the layered (dagre) family; \code{dag_layout()} wraps
g6's \code{antv-dagre} with the handful of knobs that matter and validates them,
rather than exposing g6R's full (mostly unsuitable) layout surface.
}
\details{
The default flows top-to-bottom (\code{rankdir = "TB"}), the natural reading for
the common small board. On large boards a top-down layout collides the
sibling labels (blockr nodes label below the icon) and sprawls horizontally;
switching to \code{rankdir = "LR"} there spreads siblings vertically and reserves
label width along the rank axis, removing overlap and keeping the graph much
narrower.
}
