% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g6r.R
\name{new_nodes_layout}
\alias{new_nodes_layout}
\title{Lay out a batch of newly added nodes}
\usage{
new_nodes_layout(
  ids,
  links = NULL,
  base_x = 150,
  base_y = 150,
  ranksep = 130,
  nodesep = 150
)
}
\arguments{
\item{ids}{Character vector of block IDs being added (order is preserved in
the returned coordinates).}

\item{links}{Board links from the same update. Only links whose \code{from} and
\code{to} are both in \code{ids} contribute to the layout; cross-links to existing
nodes are ignored.}

\item{base_x, base_y}{Anchor coordinates (top-centre of the new cluster),
typically the cursor position.}

\item{ranksep, nodesep}{Vertical spacing between layers and horizontal spacing
between siblings within a layer, in pixels.}
}
\value{
A list with numeric vectors \code{x} and \code{y}, aligned to \code{ids}.
}
\description{
Computes explicit \code{x}/\code{y} coordinates for a set of newly added nodes so they
can be inserted without re-running the global graph layout (which would move
existing nodes). When the new nodes are interconnected by \code{links}, they are
arranged as a small top-to-bottom layered DAG mirroring the main
\code{\link[g6R:antv_dagre_layout]{g6R::antv_dagre_layout()}} direction. Nodes with no internal structure fall
back to a simple vertical stack.
}
\keyword{internal}
