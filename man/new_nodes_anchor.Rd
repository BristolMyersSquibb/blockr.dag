% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g6r.R
\name{new_nodes_anchor}
\alias{new_nodes_anchor}
\title{Pick an anchor point for a batch of newly added nodes}
\usage{
new_nodes_anchor(ids, links, proxy, ranksep = 130)
}
\arguments{
\item{ids}{Character vector of block IDs being added (order is preserved in
the returned coordinates).}

\item{links}{Board links from the same update. Only links whose \code{from} and
\code{to} are both in \code{ids} contribute to the layout; cross-links to existing
nodes are ignored.}

\item{proxy}{A \code{g6_proxy} object.}
}
\value{
A list with scalar \code{x} and \code{y}.
}
\description{
When the batch connects to existing nodes (an append/paste onto the current
graph), the cluster is anchored centred just below its existing parents so it
reads as a continuation of the graph. Otherwise it falls back to the last
known cursor position (a blank-canvas add).
}
\keyword{internal}
